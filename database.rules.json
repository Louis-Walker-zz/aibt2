{
  "rules": {
    "answers": {
      ".read": true,

      "$answer": {
        ".write": true,
        ".validate": "newData.hasChildren(['answer', 'country_code', 'country_name', 'timestamp'])"
      }
    }
  }
}
